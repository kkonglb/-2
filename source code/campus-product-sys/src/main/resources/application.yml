# 服务配置
server:
  port: 21090
  servlet:
    context-path: /api/campus-product-sys/v1.0
# API地址前缀，用于接口拦截放行
my-server:
  api-context-path: /api/campus-product-sys/v1.0

# 文件配置
file:
  # 图片存储路径 (Windows本地测试可改为 D:/pic/，Linux生产环境保持 /www/wwwroot/...)
  upload-path: /www/wwwroot/campus-product-sys/pic
  # 图片访问前缀 (生产环境请改为服务器IP或域名，本地为http://localhost:21090，例如 http://47.xxx.xxx.xxx:21090)
  view-url: http://123.56.187.166:21090 
#MySQL与文件上传限制配置
spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/campus-2?characterEncoding=utf8&useSSL=false&serverTimezone=GMT%2B8&allowPublicKeyRetrieval=true
    username: campus-2
    password: kong326327
# 让 Mybatis 在 Mapper目录下找到 XML 映射文件
mybatis:
  mapper-locations: classpath:mapper/*.xml




